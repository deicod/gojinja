package runtime

import (
	"encoding/gob"

	"github.com/deicod/gojinja/nodes"
)

func init() {
	gob.Register(&BytecodeArtifact{})
	gob.Register(&nodes.Template{})
	gob.Register(&nodes.Output{})
	gob.Register(&nodes.Extends{})
	gob.Register(&nodes.For{})
	gob.Register(&nodes.If{})
	gob.Register(&nodes.Macro{})
	gob.Register(&nodes.CallBlock{})
	gob.Register(&nodes.FilterBlock{})
	gob.Register(&nodes.Spaceless{})
	gob.Register(&nodes.With{})
	gob.Register(&nodes.Namespace{})
	gob.Register(&nodes.Export{})
	gob.Register(&nodes.Trans{})
	gob.Register(&nodes.Block{})
	gob.Register(&nodes.Include{})
	gob.Register(&nodes.Import{})
	gob.Register(&nodes.FromImport{})
	gob.Register(&nodes.ImportName{})
	gob.Register(&nodes.ExprStmt{})
	gob.Register(&nodes.Assign{})
	gob.Register(&nodes.AssignBlock{})
	gob.Register(&nodes.Do{})
	gob.Register(&nodes.BinExpr{})
	gob.Register(&nodes.UnaryExpr{})
	gob.Register(&nodes.Await{})
	gob.Register(&nodes.Name{})
	gob.Register(&nodes.NSRef{})
	gob.Register(&nodes.BaseLiteral{})
	gob.Register(&nodes.Const{})
	gob.Register(&nodes.TemplateData{})
	gob.Register(&nodes.Tuple{})
	gob.Register(&nodes.List{})
	gob.Register(&nodes.Dict{})
	gob.Register(&nodes.Pair{})
	gob.Register(&nodes.Keyword{})
	gob.Register(&nodes.CondExpr{})
	gob.Register(&nodes.FilterTestCommon{})
	gob.Register(&nodes.Filter{})
	gob.Register(&nodes.Test{})
	gob.Register(&nodes.Call{})
	gob.Register(&nodes.Getitem{})
	gob.Register(&nodes.Getattr{})
	gob.Register(&nodes.Slice{})
	gob.Register(&nodes.Concat{})
	gob.Register(&nodes.Compare{})
	gob.Register(&nodes.Operand{})
	gob.Register(&nodes.Mul{})
	gob.Register(&nodes.Div{})
	gob.Register(&nodes.FloorDiv{})
	gob.Register(&nodes.Add{})
	gob.Register(&nodes.Sub{})
	gob.Register(&nodes.Mod{})
	gob.Register(&nodes.Pow{})
	gob.Register(&nodes.And{})
	gob.Register(&nodes.Or{})
	gob.Register(&nodes.Not{})
	gob.Register(&nodes.Neg{})
	gob.Register(&nodes.Pos{})
	gob.Register(&nodes.EnvironmentAttribute{})
	gob.Register(&nodes.ExtensionAttribute{})
	gob.Register(&nodes.ImportedName{})
	gob.Register(&nodes.InternalName{})
	gob.Register(&nodes.MarkSafe{})
	gob.Register(&nodes.MarkSafeIfAutoescape{})
	gob.Register(&nodes.ContextReference{})
	gob.Register(&nodes.DerivedContextReference{})
	gob.Register(&nodes.Continue{})
	gob.Register(&nodes.Break{})
	gob.Register(&nodes.Scope{})
	gob.Register(&nodes.OverlayScope{})
	gob.Register(&nodes.EvalContextModifier{})
	gob.Register(&nodes.ScopedEvalContextModifier{})
}
